{% extends 'base.html.twig' %}

{% block title %}Stats production{% endblock %}

{% block body %}
  </style>
  <div class="container">
  <div class="row mb-3 mt-5">
    <div class="col-lg-6">
      {{ render_chart(chart1) }}
    </div>
    <div class="col-lg-6">
      {{ render_chart(chart3) }}
    </div>
  </div>
  <br>
  <div class="container">
    <div class="row mb-3 mt-5 align-items-end">
      <div class="col-lg-4">
        {{ render_chart(chart2) }}
      </div>
      <div class="col-lg-4">
        {{ render_chart(chart4) }}
      </div>
      <div class="col-lg-4">
        {{ render_chart(chart5) }}
      </div>
    </div>
  </div>
{% endblock %}